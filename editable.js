// const $tableID = $('#table');
// const $saveBtn = $('#save-btn');
// const $EXPORT = $('#table');
//
// $('.table-add').on('click', 'i', () => {
//    const $clone = $tableID.find('tbody tr').last().clone(true).removeClass('hide table-line');
//
//
// $saveBtn.on('click', '.table-up', function () {
//
//  });
//
// // A few jQuery helpers for exporting only jQuery.fn.pop= [].pop;
//  jQuery.fn.shift = [].shift; $BTN.on('click', () => {
//   const $rows = $tableID.find('tr:not(:hidden)'); const headers = []; const data = [];
//
// // Turn all existing rows into a loopable
// $rows.each(function () {
//   const $td = $(this).find('td'); const h = {}; // Use the
// data.push(h); });
// // Output the result
// $EXPORT.text(JSON.stringify(data)); });
